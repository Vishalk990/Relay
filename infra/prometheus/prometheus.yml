global:
  # interval to pull metrics 
  scrape_interval: 1m

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
  - job_name: relay-backend
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8080"]